class Site {
    constructor(name, pins, margin) {
        this.name = name;
        this.pins = pins;
        this.margin = margin;
    }

    setName(newName) {
        this.name = name;
    }

    setMargin(newMargin) {
        this.margin = newMargin;
    }

    setPins(newPins) {
        this.pins = pins;
    }
}

module.exports = Site;